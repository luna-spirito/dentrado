[package]
name = "dentrado-macros"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Proc-macro support for dentrado (derive(Localizable), gear macros)"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
