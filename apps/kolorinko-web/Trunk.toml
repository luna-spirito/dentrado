[build]
target = "index.html"
dist = "dist"
# Trunk emits root-relative URLs (`/kolorinko-web-….js` & co) for the hashed
# loader tags, matching the root-relative paths the HTTP layer answers.
public_url = "/"
